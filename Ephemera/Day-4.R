# Thursday Sept. 15, 2016

## Don't use `fix()`

Use `View()` instead.
```r
View(College)
```

## Subsets

The book has you use `fix()`.

```{r}
library(ISLR)
data(Auto)
head(Auto)
```

### Missing data

```{r}
```